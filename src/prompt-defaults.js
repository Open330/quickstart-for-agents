export const DEFAULT_AGENT_TITLE = "Set up this project";
export const DEFAULT_AGENT_LANGUAGE = "prompt";
export const DEFAULT_AGENT_PROMPT = `Set up this project in the current workspace. Read the repository instructions and documentation, use the existing toolchain, and verify the setup works.`;
export const DEFAULT_FOOTER_TEXT = "copy prompt · paste into your coding agent · verify the setup";
